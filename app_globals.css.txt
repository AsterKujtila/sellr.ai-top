@import url('https://fonts.googleapis.com/css2?family=Figtree:wght@400;500;600;700;800&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144;600;9..144,700&display=swap');
@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&display=swap');

@layer base {
  :root {
    /* Premium color palette - 2026 aesthetic */
    --background: 8 18% 4%;
    --foreground: 40 10% 96%;
    --surface: 12 25% 8%;
    --surface-variant: 15 30% 12%;
    --primary: 12 100% 48%;
    --primary-dark: 12 100% 40%;
    --primary-light: 12 100% 56%;
    --secondary: 250 95% 55%;
    --accent: 45 100% 52%;
    --muted: 40 8% 45%;
    --muted-foreground: 40 8% 60%;
    --border: rgba(255, 255, 255, 0.08);
    --border-light: rgba(255, 255, 255, 0.12);
    --ring: 12 100% 48%;
    --destructive: 0 84% 60%;
    --success: 142 71% 45%;
    --warning: 38 92% 50%;
    --info: 199 89% 48%;
    --radius: 12px;
    --radius-lg: 16px;
    --radius-xl: 20px;
    --radius-full: 9999px;

    /* Shadows */
    --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
    --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
    --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
    --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
    --shadow-2xl: 0 25px 50px -12px rgb(0 0 0 / 0.25);

    /* Fonts */
    --font-sans: 'Figtree', system-ui, sans-serif;
    --font-serif: 'Fraunces', Georgia, serif;
    --font-mono: 'JetBrains Mono', monospace;

    /* Transitions */
    --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
    --transition-base: 200ms cubic-bezier(0.4, 0, 0.2, 1);
    --transition-slow: 300ms cubic-bezier(0.4, 0, 0.2, 1);
  }

  * {
    @apply border-border;
  }

  html {
    @apply scroll-smooth;
    color-scheme: dark;
  }

  body {
    @apply bg-background text-foreground font-sans antialiased overflow-hidden;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  input, textarea, select {
    @apply bg-surface text-foreground;
  }

  ::placeholder {
    @apply text-muted-foreground;
  }

  ::-webkit-scrollbar {
    width: 6px;
    height: 6px;
  }

  ::-webkit-scrollbar-track {
    @apply bg-surface-variant;
  }

  ::-webkit-scrollbar-thumb {
    @apply bg-border rounded-full hover:bg-border-light;
  }
}

@layer components {
  /* Premium Glass Morphism */
  .glass-panel {
    @apply bg-surface/40 backdrop-blur-xl border border-border;
    background: rgba(20, 44, 60, 0.4);
  }

  .glass-panel-strong {
    @apply bg-surface/60 backdrop-blur-2xl border border-border-light;
    background: rgba(20, 44, 60, 0.6);
  }

  .glass-button {
    @apply bg-surface/40 backdrop-blur-md border border-border hover:bg-surface/50 hover:border-border-light transition-all duration-200;
  }

  /* Premium Input */
  .premium-input {
    @apply w-full px-4 py-3 rounded-lg bg-surface border border-border text-foreground text-sm transition-all duration-200;
    @apply placeholder:text-muted-foreground focus:outline-none focus:border-primary focus:ring-2 focus:ring-primary/20;
  }

  .premium-textarea {
    @apply w-full px-4 py-3 rounded-lg bg-surface border border-border text-foreground text-sm transition-all duration-200 resize-none;
    @apply placeholder:text-muted-foreground focus:outline-none focus:border-primary focus:ring-2 focus:ring-primary/20;
  }

  /* Premium Button Variants */
  .btn-primary {
    @apply px-6 py-3 rounded-lg bg-primary text-white font-semibold transition-all duration-200;
    @apply hover:bg-primary-dark active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed;
  }

  .btn-secondary {
    @apply px-6 py-3 rounded-lg bg-surface-variant border border-border-light text-foreground font-semibold transition-all duration-200;
    @apply hover:bg-surface-variant/80 active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed;
  }

  .btn-ghost {
    @apply px-6 py-3 rounded-lg text-foreground font-semibold transition-all duration-200;
    @apply hover:bg-surface-variant active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed;
  }

  .btn-icon {
    @apply inline-flex items-center justify-center w-10 h-10 rounded-lg transition-all duration-200;
    @apply hover:bg-surface-variant active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed;
  }

  /* Premium Cards */
  .card {
    @apply glass-panel rounded-xl p-6 transition-all duration-300;
  }

  .card-interactive {
    @apply card hover:glass-panel-strong hover:shadow-lg cursor-pointer;
  }

  /* Text Utilities */
  .text-gradient {
    @apply bg-gradient-to-r from-primary via-secondary to-accent bg-clip-text text-transparent;
  }

  .text-sm-mono {
    @apply text-xs font-mono tracking-wider text-muted-foreground uppercase;
  }

  /* Premium Animations */
  .animate-fade-in {
    animation: fadeIn 0.5s ease-out;
  }

  .animate-slide-up {
    animation: slideUp 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .animate-slide-in-left {
    animation: slideInLeft 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .animate-scale-in {
    animation: scaleIn 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .animate-pulse-soft {
    animation: pulseSoft 2s ease-in-out infinite;
  }

  /* Gradient Backgrounds */
  .gradient-dark {
    background: linear-gradient(135deg, #0a1215 0%, #0f1820 50%, #1a2635 100%);
  }

  .gradient-brand {
    background: linear-gradient(135deg, #ff6b35 0%, #ff8c42 50%, #ffa500 100%);
  }

  .gradient-aurora {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  }

  /* Utility Classes */
  .no-scrollbar {
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }

  .safe-area-bottom {
    padding-bottom: env(safe-area-inset-bottom);
  }

  .blur-backdrop {
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }

  .blur-backdrop-strong {
    backdrop-filter: blur(30px);
    -webkit-backdrop-filter: blur(30px);
  }
}

@layer utilities {
  .flex-center {
    @apply flex items-center justify-center;
  }

  .flex-between {
    @apply flex items-center justify-between;
  }

  .grid-auto {
    @apply grid auto-cols-minmax(250px, 1fr) gap-4;
  }

  .truncate-lines-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .truncate-lines-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
}

/* Premium Keyframes */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(16px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideInLeft {
  from {
    opacity: 0;
    transform: translateX(-16px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes pulseSoft {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.7;
  }
}

@keyframes shimmer {
  0% {
    background-position: -1000px 0;
  }
  100% {
    background-position: 1000px 0;
  }
}

@keyframes dotPulse {
  0%, 100% {
    opacity: 0.4;
    transform: translateY(0);
  }
  50% {
    opacity: 1;
    transform: translateY(-8px);
  }
}

/* Prose for AI responses */
.prose-premium {
  @apply text-foreground leading-relaxed;
}

.prose-premium p {
  @apply mb-4;
}

.prose-premium strong {
  @apply font-semibold text-primary;
}

.prose-premium em {
  @apply font-serif italic text-secondary;
}

.prose-premium code {
  @apply font-mono text-sm px-2 py-1 rounded bg-surface-variant text-accent;
}

.prose-premium pre {
  @apply bg-surface-variant rounded-lg p-4 overflow-x-auto mb-4 border border-border;
}

.prose-premium h1 {
  @apply font-serif text-3xl font-bold mb-4 mt-6;
}

.prose-premium h2 {
  @apply font-serif text-2xl font-bold mb-3 mt-5;
}

.prose-premium h3 {
  @apply font-serif text-xl font-semibold mb-2 mt-4;
}

.prose-premium ul {
  @apply list-disc list-inside mb-4 space-y-2;
}

.prose-premium ol {
  @apply list-decimal list-inside mb-4 space-y-2;
}

.prose-premium li {
  @apply mb-1;
}

.prose-premium blockquote {
  @apply border-l-4 border-primary pl-4 italic opacity-80 mb-4;
}

.prose-premium a {
  @apply text-primary underline hover:text-primary-light transition-colors;
}