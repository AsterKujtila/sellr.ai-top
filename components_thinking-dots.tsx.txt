'use client';

export function ThinkingDots() {
  return (
    <div className="flex items-center gap-2 px-4 py-3 bg-[rgba(255,255,255,0.07)] rounded-2xl rounded-bl-sm w-fit">
      {[0, 1, 2].map((i) => (
        <div
          key={i}
          className="w-2 h-2 rounded-full bg-[#c4724a] animate-dot-bounce"
          style={{
            animationDelay: `${i * 0.2}s`,
          }}
        />
      ))}
    </div>
  );
}